export interface IGhost {
    x?: number,
    y?: number,
    startX?:number,
    startY?:number,
    last?: string,
    prevBlock:number,
    marker: number,
    color: string,
    eatable:boolean,
}